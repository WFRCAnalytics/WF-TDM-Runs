scenario_id: base
description: >
  Converged 2023 base network -- no changes. Reused from the v10.0-beta.2-2023
  run (same TDM ref, same raw folder household-location-burden's own "base"
  scenario also points at) rather than re-run from scratch; registered here
  via import-manual-run so this run set has its own record of it.
use_baseline_scenario_name: true

baseline_control_center: "_ControlCenter - Base_2023.block"

manual_scenario_folder: "Scenarios/v10.0-beta.2/v10.0-beta.2-2023"
