scenario_id: S07
description: "HH x2 and EMP x2 â€” small district scope"
# prep_script:   # optional: path to a Python script relative to the run_set dir

input_files:
  WFRC_SEFile: "inputs/SE_S07.csv"
  MAG_SEFile: "inputs/SE_S07.csv"

overrides:
  RunDescription: "S07 - localized HH x2 + EMP x2 (smldst)"
# prep_script:   # optional: path to a Python script relative to the run_set dir

# Run manually outside the CLI (see CLAUDE.md block-format blocker). Used by
# import-manual-run(-set) to locate raw outputs to curate into runs/.
manual_scenario_folder: "Scenarios/non-motorized-2023/BY_2019_SensitivityTest_07"
