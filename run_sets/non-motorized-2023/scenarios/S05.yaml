scenario_id: S05
description: "EMP x4 â€” small district + specific TAZ scope"
# prep_script:   # optional: path to a Python script relative to the run_set dir

input_files:
  WFRC_SEFile: "inputs/SE_S05.csv"
  MAG_SEFile: "inputs/SE_S05.csv"

overrides:
  RunDescription: "S05 - localized EMP x4"
# prep_script:   # optional: path to a Python script relative to the run_set dir

# Run manually outside the CLI (see CLAUDE.md block-format blocker). Used by
# import-manual-run(-set) to locate raw outputs to curate into runs/.
manual_scenario_folder: "Scenarios/non-motorized-2023/BY_2019_SensitivityTest_05"
