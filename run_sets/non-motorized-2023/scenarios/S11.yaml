scenario_id: S11
description: "Future Today Centerized â€” SE_2050 transit corridors, small district scope"
# prep_script:   # optional: path to a Python script relative to the run_set dir

input_files:
  WFRC_SEFile: "data/inputs/SE_S11.csv"
  MAG_SEFile: "data/inputs/SE_S11.csv"

overrides:
  RunDescription: "S11 - Future Today Centerized SE_2050_transit_corridors (smldst)"
# prep_script:   # optional: path to a Python script relative to the run_set dir

# Scenario outputs fully replace the run set's outputs.include -- this
# scenario also needs its SE_File for the district-level centerization chart.
outputs:
  include:
    - "4_ModeChoice/2_DetailedTripMatrices/*_ZoneSummary_TripsByMode.csv"
    - "0_InputProcessing/SE_File_*.dbf"
  max_file_size_mb: 45
