scenario_id: S10
description: "Future Today â€” full SE_2050 substitution, small district scope"
# prep_script:   # optional: path to a Python script relative to the run_set dir

input_files:
  WFRC_SEFile: "inputs/SE_S10.csv"
  MAG_SEFile: "inputs/SE_S10.csv"

overrides:
  RunDescription: "S10 - Future Today SE_2050 (smldst)"
# prep_script:   # optional: path to a Python script relative to the run_set dir

# Scenario outputs fully replace the run set's outputs.include -- this
# scenario also needs its SE_File for the district-level centerization chart.
outputs:
  include:
    - file: "4_ModeChoice/2_DetailedTripMatrices/*_ZoneSummary_TripsByMode.csv"
    - file: "0_InputProcessing/SE_File_*.dbf"
  max_file_size_mb: 45

# Run manually outside the CLI (see CLAUDE.md block-format blocker). Used by
# import-manual-run(-set) to locate raw outputs to curate into runs/.
manual_scenario_folder: "Scenarios/non-motorized-2023/BY_2019_SensitivityTest_10"
