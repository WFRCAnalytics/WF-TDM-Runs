scenario_id: S01
description: "HH x2 — small district scope"
# prep_script:   # optional: path to a Python script relative to the run_set dir

overrides:
  RunDescription: "S01 - localized HH x2"
  WFRC_SEFile: '..\..\..\run_sets\non-motorized-2023\inputs\SE_S01.csv'
  MAG_SEFile: '..\..\..\run_sets\non-motorized-2023\inputs\SE_S01.csv'
# prep_script:   # optional: path to a Python script relative to the run_set dir

# Run manually outside the CLI (see CLAUDE.md block-format blocker). Used by
# import-manual-run(-set) to locate raw outputs to curate into runs/.
manual_scenario_folder: "Scenarios/non-motorized-2023/BY_2019_SensitivityTest_01"
