{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "run_set.schema.json",
  "title": "Run set",
  "type": "object",
  "required": ["run_set_id", "tdm_ref", "baseline_control_center"],
  "additionalProperties": false,
  "properties": {
    "run_set_id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*$",
      "description": "Unique, stable slug for this run set. Never reuse after retiring a run set."
    },
    "description": {
      "type": "string"
    },
    "author": {
      "type": "string",
      "description": "Who prepared/maintains this run set, shown in reports under its own heading rather than as a single page-wide author -- since different run sets in this repo may be maintained by different people."
    },
    "tdm_ref": {
      "type": "string",
      "description": "Git tag, branch, or commit SHA in the TDM submodule that scenarios in this run set run against, unless a scenario overrides it."
    },
    "baseline_control_center": {
      "type": "string",
      "description": "Filename of the default Control Center template in the TDM's Scenarios/_defaults/ library that this run set's scenarios diff against, unless a scenario overrides it."
    },
    "input_files": {
      "type": "object",
      "description": "Input file paths shared by all scenarios in this run set, relative to the run_set directory. Resolved to absolute paths by the framework before writing the Control Center.",
      "additionalProperties": { "type": "string" }
    },
    "overrides": {
      "type": "object",
      "description": "Control Center key/value overrides shared by every scenario in this run set, applied before any scenario-specific overrides.",
      "additionalProperties": true
    },
    "driver_script": {
      "type": "string",
      "description": "Path to a custom driver script (a _HailMary_1Subfolder.s variant), relative to the run_set directory, unless a scenario overrides it. When declared, this one file is staged (copied, keeping its own filename) into the per-run scenario folder before execution, in place of the framework's default (config/framework.yaml's default_driver_script, from the TDM's Scenarios/_default/). Companion/modified step scripts are NOT auto-staged and stay in place, referenced from this file by a relative path back to their own location. Must reference default TDM step scripts as '..\\..\\..\\2_ModelScripts\\...' (three levels up, matching the per-run scenario folder's depth)."
    },
    "prep_script": {
      "type": "string",
      "description": "Path to a Python prep script, relative to the run_set directory. Run once per scenario before model execution. Receives --run-set-dir and --scenario-id as arguments."
    },
    "report_snapshot_script": {
      "type": "string",
      "description": "Path to a Python script, relative to the run_set directory, that freezes whatever this run set's reports need from runs/ into run_sets/<id>/snapshot/ so raw curated outputs can later be purged. Invoked by `tdmruns snapshot-run-set` with --run-set-dir and --snapshot-dir arguments."
    },
    "outputs": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "include": {
          "type": "array",
          "items": {
            "type": "object",
            "oneOf": [
              {
                "additionalProperties": false,
                "required": ["datafile"],
                "properties": {
                  "datafile": { "type": "string", "description": "Glob pattern, evaluated against each output file's path relative to the scenario folder." },
                  "columns": {
                    "type": "array",
                    "items": { "type": "string" },
                    "minItems": 1,
                    "description": "Column names (matched against the CSV's header row) to keep. The matched file is written as a column-filtered copy named '<stem>_filtered.csv' instead of a byte-for-byte copy -- for a large summary CSV where only a handful of columns are actually read downstream (e.g. Cube Voyager's 'TAZ-Based Metrics.csv', ~18 columns and ~200 MB, comfortably over any reasonable size ceiling). Omit to copy the matched file(s) byte-for-byte."
                  }
                }
              },
              {
                "additionalProperties": false,
                "required": ["matrix", "tabs"],
                "properties": {
                  "matrix": { "type": "string", "description": "Glob pattern, evaluated against each output file's path relative to the scenario folder, matching a Cube Voyager .mtx matrix file." },
                  "tabs": {
                    "type": "array",
                    "items": { "type": "string" },
                    "minItems": 1,
                    "description": "Table/core names to extract from the matched .mtx file. Only these named tables are kept -- for large multi-table Cube matrices (e.g. skims, several hundred MB) where only one or two tables are actually needed downstream. Requires Voyager to be available at curation time (config/local.yaml's Voyager_EXE)."
                  },
                  "format": {
                    "type": "string",
                    "enum": ["omx", "mtx"],
                    "default": "omx",
                    "description": "Output format for the extracted tables, written as '<stem>.<format>'. 'omx' (default): an OMX file, readable directly in Python via the openmatrix package. 'mtx': converted back to Cube's own native TPP matrix format via CONVERTMAT -- for when the curated output is meant to be read by Cube Voyager itself, not Python."
                  }
                }
              },
              {
                "additionalProperties": false,
                "required": ["network", "fields"],
                "properties": {
                  "network": { "type": "string", "description": "Glob pattern, evaluated against each output file's path relative to the scenario folder, matching a Cube Voyager .net network file." },
                  "fields": {
                    "type": "array",
                    "items": { "type": "string" },
                    "minItems": 1,
                    "description": "Link attribute field names to keep from the matched .net file (plus geometry, for shp/geojson formats) -- a .net file is Voyager's proprietary format and typically carries far more link attributes than a report needs. Requires Voyager to be available at curation time (config/local.yaml's Voyager_EXE)."
                  },
                  "format": {
                    "type": "string",
                    "enum": ["net", "shp", "geojson"],
                    "default": "geojson",
                    "description": "Output format for the field-filtered network, written as '<stem>.<format>' ('shp' is written as a single '<stem>.shp.zip' bundling the shapefile's sidecar files). 'geojson' (default): readable directly in Python via geopandas, reprojected to EPSG:4326 (WGS84) to match GeoJSON convention and this codebase's own vt_CreateGeoJsons.py precedent. 'shp': a field-filtered Esri shapefile, zipped, kept in the source geometry's native CRS (no reprojection). 'net': Cube's own native .net format, field-filtered via NETWORK's EXCLUDE= -- for when the curated output is meant to be read by Cube Voyager itself, not Python/GIS tools."
                  }
                }
              }
            ]
          }
        },
        "max_file_size_mb": { "type": "number", "exclusiveMinimum": 0 }
      }
    }
  }
}
